mutation LoginWithGoogle($idToken: String!) {
  loginWithGoogle(idToken: $idToken) {
    access_token
    refresh_token
    user {
      name
      email
      _id
      active
      avatar
    }
  }
}
